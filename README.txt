The private key file should be .der file and it should be named private_key
In Udp mode the Clientport can be same as the udpPort
But in tcp mode it should be different with port
In udp mode blocksize is suggested to be equal or less than 5000 
